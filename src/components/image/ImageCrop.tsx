type Props = {
    onBack: () => void
}
const ImageCrop = (props: Props) => {
     
    const { onBack } = props

    return(
        <>
            From an image crop
        </>
    )

}

export default ImageCrop;
